[gd_scene format=3 uid="uid://bb3ffhw01qtbs"]

[ext_resource type="Script" uid="uid://bdfk612hrif2k" path="res://Scripts/waterReflections.gd" id="1_5v6ty"]

[sub_resource type="PlaneMesh" id="PlaneMesh_g4v7s"]

[sub_resource type="Shader" id="Shader_aq4x0"]
code = "shader_type spatial;
render_mode blend_mix, depth_draw_opaque, cull_disabled;

// Color Uniforms
uniform vec4 deep_color : source_color = vec4(0.12, 0.43, 0.66, 0.72);
uniform vec4 shallow_color : source_color = vec4(0.31, 0.76, 0.85, 0.90);
uniform vec4 foam_color : source_color = vec4(1.0, 1.0, 1.0, 1.0);

// Reflection Settings
uniform sampler2D reflection_texture : hint_default_black, filter_linear_mipmap;
uniform float reflection_strength : hint_range(0.0, 1.0) = 0.35;
uniform float reflection_distortion : hint_range(0.0, 0.05) = 0.008;

// Wave & Glitter Settings
uniform sampler2D water_noise : hint_default_white, repeat_enable;
uniform float wave_scale = 0.034;
uniform vec2 wave_speed = vec2(0.006, 0.004);
uniform float color_steps = 3.0;

// Foam & Depth Settings
uniform float foam_depth = 0.6;
uniform sampler2D depth_texture : hint_depth_texture, filter_linear_mipmap;

// Utility function to quantize values into stepped bands
float quantize(float value, float levels) {
	float clamped = clamp(value, 0.0, 1.0);
	return floor(clamped * levels) / max(levels - 1.0, 1.0);
}

// Calculate animated noise waves
float wave_height(vec2 world_pos_xz, float time) {
	vec2 uvw = world_pos_xz * wave_scale;
	float n1 = texture(water_noise, uvw + time * wave_speed).r;
	float n2 = texture(water_noise, uvw * 1.9 - time * wave_speed * 0.7 + vec2(0.27, 0.61)).r;
	return n1 * 0.6 + n2 * 0.4;
}

void fragment() {
	// 1. World Position calculation for seamless tiling across meshes
	vec3 world_pos = (INV_VIEW_MATRIX * vec4(VERTEX, 1.0)).xyz;
	
	// 2. Base Water Color (Quantized Noise)
	float waves = wave_height(world_pos.xz, TIME);
	float banded = quantize(waves, color_steps);
	vec3 col = mix(deep_color.rgb, shallow_color.rgb, banded);

	// 3. Planar Reflections (Screen UV with wave distortion)
	vec2 refl_uv = SCREEN_UV;
	// Use wave height to slightly distort reflection UVs for rippling water
	refl_uv += (waves - 0.5) * reflection_distortion;
	vec3 reflection_col = texture(reflection_texture, refl_uv).rgb;
	
	// Mix reflection into base water color before glints and foam
	col = mix(col, reflection_col, reflection_strength);

	// 4. Glitter / Caustics Glint
	vec2 glint_uv = world_pos.xz * wave_scale * 2.3 + TIME * wave_speed * 1.6;
	float glint = texture(water_noise, glint_uv).r;
	glint = step(0.86, quantize(glint, 8.0));
	col = mix(col, foam_color.rgb, glint * 0.8);

	// 5. Edge Foam using Godot's Depth Texture
	// Read raw depth buffer at screen position
	float depth_raw = texture(depth_texture, SCREEN_UV).r;
	
	// Convert NDC depth to linear view space Z distance
	vec4 upos = INV_PROJECTION_MATRIX * vec4(SCREEN_UV * 2.0 - 1.0, depth_raw, 1.0);
	float scene_depth_z = -upos.z / upos.w;
	float fragment_depth_z = -VERTEX.z; // Fragment depth in view space
	
	float depth_delta = scene_depth_z - fragment_depth_z;

	// Add noise wobble to the foam threshold
	vec2 foam_uv = world_pos.xz * 0.15 + TIME * wave_speed * 3.0;
	float foam_noise = texture(water_noise, foam_uv).r;
	float threshold = foam_depth * (0.6 + 0.5 * foam_noise);
	
	// Calculate soft edge foam
	float foam = 1.0 - smoothstep(0.0, threshold, depth_delta);
	
	// Quantize foam into discrete bands
	float foam_band = quantize(foam, 3.0);
	col = mix(col, foam_color.rgb, foam_band);

	// Output
	ALBEDO = col;
	ALPHA = mix(deep_color.a, shallow_color.a, foam_band);
	ROUGHNESS = 0.1;
	SPECULAR = 0.2;
}"

[sub_resource type="ViewportTexture" id="ViewportTexture_5v6ty"]
viewport_path = NodePath("SubViewport")

[sub_resource type="FastNoiseLite" id="FastNoiseLite_3sehy"]
frequency = 0.0258

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_ev8si"]
noise = SubResource("FastNoiseLite_3sehy")
seamless = true

[sub_resource type="ShaderMaterial" id="ShaderMaterial_hjwib"]
resource_local_to_scene = true
render_priority = 0
shader = SubResource("Shader_aq4x0")
shader_parameter/deep_color = Color(0.12, 0.43, 0.66, 0.72)
shader_parameter/shallow_color = Color(0.31, 0.76, 0.85, 0.9)
shader_parameter/foam_color = Color(1, 1, 1, 1)
shader_parameter/reflection_texture = SubResource("ViewportTexture_5v6ty")
shader_parameter/reflection_strength = 0.35
shader_parameter/reflection_distortion = 0.008
shader_parameter/water_noise = SubResource("NoiseTexture2D_ev8si")
shader_parameter/wave_scale = 0.029
shader_parameter/wave_speed = Vector2(0.006, 0.004)
shader_parameter/color_steps = 4.27
shader_parameter/foam_depth = 0.6

[node name="Water" type="Node3D" unique_id=1750594137 node_paths=PackedStringArray("reflection_camera", "water_mesh")]
transform = Transform3D(2.9083884, 0, 0, 0, 1, 0, 0, 0, 2.704547, 0, 0, 0)
script = ExtResource("1_5v6ty")
reflection_camera = NodePath("SubViewport/ReflectionCamera")
water_mesh = NodePath("WaterMesh")
pitch_offset_degrees = null
depth_offset = null

[node name="WaterMesh" type="MeshInstance3D" parent="." unique_id=142579556]
transform = Transform3D(4.1777143, 0, 0, 0, 1.0000002, 0, 0, 0, 4.0939913, 0, 0, 0)
layers = 4
mesh = SubResource("PlaneMesh_g4v7s")
surface_material_override/0 = SubResource("ShaderMaterial_hjwib")

[node name="SubViewport" type="SubViewport" parent="." unique_id=1636196440]
transparent_bg = true
size = Vector2i(384, 216)
render_target_update_mode = 4

[node name="ReflectionCamera" type="Camera3D" parent="SubViewport" unique_id=1717754673]
cull_mask = 1
projection = 1
size = 12.0
